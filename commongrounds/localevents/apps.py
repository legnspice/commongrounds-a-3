from django.apps import AppConfig


class LocaleventsConfig(AppConfig):
    """Configuration for the localevents application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'localevents'
